# Base production rollout — 2026-09-16

## Deployed services

- Library: https://honeycomb.teamofsilicons.com — SolidJS assets on Vercel project
  `silicon-honeycomb-library`.
- Console: https://console.honeycomb.teamofsilicons.com — SolidJS assets on Vercel
  project `silicon-honeycomb-console`.
- Backend: https://backend.honeycomb.teamofsilicons.com — dedicated AWS EC2 ARM64
  instance `i-06986627793021fb2`, public address `3.139.34.154`, `us-east-2`, account
  `234951665042`, deployed with profile `silicon-production`.
- CloudFormation stack `silicon-honeycomb-production` is `CREATE_COMPLETE`.
  SSM deployment `1eacca16-ec9f-4456-8868-fc80f29c38f0` succeeded. The Rust backend,
  two persistent Node session services and Caddy are running.
- Namecheap A records for all three hosts were added at TTL 300. A comparison
  against the preceding DNS snapshot confirmed every pre-existing record survived.

Images in the `us-east-2` ECR repositories are pinned by digest:

| Repository | Digest |
| --- | --- |
| silicon-honeycomb-backend | `sha256:2044dbda2e996403279411b8b0597bbbc674f69b167235cb5e114c8e99a3c9f6` |
| silicon-honeycomb-web | `sha256:b3fd30c32eedd7157834289db0729f5ae55048d4791d53ebc32bb6d8066bb239` |

The backend now uses source revision `8fbdb3d`; the unchanged Node session image
uses `4da1cc7`. Vercel routing and AWS deployment files are committed in `cf9ffcd`.
IAM management credentials and
independent web session/encryption keys are in Secrets Manager; no secrets were
uploaded with the static frontend assets.

The first region attempted, `us-east-1`, rejected EC2 creation at its existing
32-vCPU limit. The failed stack, unassociated address, and duplicate repositories
were removed. Its duplicate runtime secret is scheduled for recoverable deletion.
Existing ecosystem servers were not modified.

## CLI publication

GitHub release [v0.1.0](https://github.com/teamofsilicons/silicon-honeycomb/releases/tag/v0.1.0)
contains six native archives and their checksums, built from `5c1b4cd` by successful
run `35035075672`. Core, client and CLI crates version 0.1.0 were published to
crates.io in dependency order. The distributable source is unchanged between
that candidate and the deployment commit.

Verified public installations on macOS ARM64:

- Exact documented curl/bash command, SHA-256 verification, executable version,
  embedded MIT notice, current registry update check, help and unauthenticated status.
- `cargo install silicon-honeycomb-cli --version '=0.1.0' --locked` from crates.io,
  followed by the same command checks.
- Default installer registration of the real macOS launchd updater in an isolated
  installation. Its LaunchAgent and temporary home were removed after verification.
- Released native CLI reads the production IAM metadata and catalog successfully.

All six native runners tested the Rust workspace, Cargo source installation,
release executable and license notice. Public registry installation above was
performed on macOS ARM64; it is not a claim of separately repeating that public
download on every supported OS.

## Live base acceptance

- Valid HTTPS on all three domains. Caddy's first certificate download received
  an ACME 404; its automatic retry obtained the production certificate successfully.
- Backend health, contract discovery and public catalog return 200; unauthenticated
  backend identity returns 401. The production catalog is empty.
- Both frontends serve built assets and the correct per-site configuration.
  Anonymous session reads return `authenticated: false` with `private, no-store`.
- Browser inspection confirms the library's empty state and console login gate,
  with the requested paper/blue/serif Interface styling.
- Both sign-in routes redirect to the correct IAM application and same-site
  callback. State cookies are HttpOnly, Secure and SameSite=Lax.
- Invalid callbacks return 400; missing-origin writes return 403. Valid same-origin
  application creation requests without a session return 401.

## Remaining acceptance

### Resource organization selection at `8fbdb3d`

- Delegated archive reads and link publication now explicitly select the owning
  organization, including existing private entry-ID-only references. This avoids
  ambiguous organization selection for users with multiple memberships.
- Strict workspace Clippy and workspace tests passed. Storage contract doubles
  cover explicit organization selection, exact signed request bytes, and
  production/test authentication and public-download routing. These are not live
  Briefcase delegated upload tests.
- Consistent SQLite backups for all three services passed integrity checks at
  `/var/lib/silicon-honeycomb/backups/20260916T005253Z`, SSM command
  `951b5e74-a8cc-4492-987d-17e0fbed43d8`.
- Deployment `ab73e4a0-75c5-45e7-ae28-01a6b1ee47f4` succeeded. Read-only verification
  `528487aa-cf85-4b37-926e-17436162a15d` confirmed all four containers running with
  zero restarts and the expected image digests. Backend health/contracts and both
  sites' configuration/anonymous-session checks returned 200.
- The first-archive console form is live on deployment
  `silicon-honeycomb-console-1z4f7r33l-saketdev12-5675s-projects.vercel.app`.
  Its custom-domain JS matched the local built artifact byte for byte. CI for
  `2cbb2e3` completed successfully (`35041126221`).

### First archive in the registration form

The console now offers a `.tar.gz` picker during application creation. Before
registering the app it validates the archive and checks its manifest app ID.
After registration it opens Releases & publication and uploads the selected
archive. Metadata-only registration remains available for initial service setup;
a release is still mandatory before publication. File selections are transient
and must be selected again after reopening a draft.

Upload recovery retains the archive and idempotency key while the page remains
open, without re-registering the application. One-time application secrets are
shown even when the release upload fails. The 46 desktop/mobile browser checks
passed; after adjusting secret presentation, four focused desktop/mobile checks
also passed, including a lost successful upload response, replay with the same
key, and exactly one stored release. Production Briefcase upload acceptance still
requires the four effective OBO grants listed in the IAM contract review.

### Follow-up acceptance at `d0259c9`

- Both Vercel frontends now serve the header Create an app link and the console's
  organization-access explanation. A live Chrome session showed authenticated
  Carbon state on both the console and library; the library link was verified
  to open the production console. This confirms real authenticated page access,
  but not private package visibility while the catalog remains empty.
- The console's Create application button remained disabled with the expected
  missing-role explanation. The live management SDK check still reported missing
  `self.membership.read` and the four deferred Briefcase grants, at IAM revision 2.
- The public curl/bash command was rerun in an isolated home. It automatically
  configured Zsh, and a new Zsh process resolved `honeycomb` and printed `0.1.0`
  without manual sourcing. Local installer tests also verified Bash login and
  interactive startup, custom ZDOTDIR, preservation of existing configuration,
  duplicate-free reinstall and the opt-out for modifying shell files.
- The 44 desktop/mobile browser scenarios passed, with one desktop permission
  picker scenario requiring a focused rerun after an unexpected page reload
  during a concurrent production build. Both desktop and mobile reruns passed.

Vercel deployments:

- Library: `silicon-honeycomb-library-1aec008es-saketdev12-5675s-projects.vercel.app`
- Console: `silicon-honeycomb-console-fsknalely-saketdev12-5675s-projects.vercel.app`

### Production authentication rejection fix at `90fe098`

- The official IAM SDK confirmed that an invalid SLT returns HTTP 400 with
  `invalid_grant`. Honeycomb previously collapsed this into a misleading 503.
  The backend now returns 401 with the stable code and fresh-token instructions;
  unknown errors and integration failures remain unavailable responses.
- Regression tests cover both login and refresh, preserve integration failures,
  and ensure upstream diagnostic text is not exposed. Workspace tests and strict
  Clippy passed. CI `35038054939` passed for the preceding installer/web update.
- SSM deployment `24714cc2-ce28-48be-b34e-043f160876a4` succeeded. Before deployment,
  SSM `e414d531-cdaf-4878-9e09-c42cb0236eec` backed up and integrity-checked all three
  SQLite databases at `/var/lib/silicon-honeycomb/backups/20260916T000650Z`.
- The released 0.1.0 CLI against production now reports HTTP 401 `invalid_grant`
  for a deliberately invalid token, explains recovery, and remains signed out.
  Backend health/contracts and both frontend configuration endpoints return 200.
  The existing authenticated library browser session survived deployment.

This rollout establishes the hosted base and CLI distribution. It does not prove
authenticated application creation, real private catalog visibility, public review
activation, shared test lifecycles, or live archive/logo delivery. IAM's required
membership grants and remaining contracts are documented in
`docs/IAM-CONTRACT-REVIEW.md`. Briefcase setup is explicitly deferred until after
the base. Complete those integrations and their authenticated end-to-end tests
before describing the full original product goal as complete.
